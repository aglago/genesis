# @genesis/dashboard

Admin dashboard with sidebar navigation, analytics stat cards, user management tables, and settings pages.

## Dependencies

Requires `@genesis/auth` for protected admin routes.

## Configuration

```typescript
dashboard({ title: "Admin Panel" })
```
